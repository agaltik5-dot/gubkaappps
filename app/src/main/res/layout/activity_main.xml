<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ui_bg"
    tools:context=".ui.MainActivity">

    <!-- 1. КОНТЕЙНЕР ДЛЯ ФРАГМЕНТОВ -->
    <!-- Сюда подставляются ScheduleFragment или ProfileFragment -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragment_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 2. НИЖНЯЯ ПАРЯЩАЯ ПАНЕЛЬ С КНОПКАМИ -->
    <androidx.cardview.widget.CardView
        android:id="@+id/card_nav"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_marginHorizontal="24dp"
        android:layout_marginBottom="24dp"
        app:cardBackgroundColor="@color/ui_surface"
        app:cardCornerRadius="32dp"
        app:cardElevation="8dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="6dp">

            <!-- Слой 1: Разделители (самый нижний) -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center_vertical"
                android:orientation="horizontal"
                android:weightSum="4">

                <View
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    android:layout_weight="1" />

                <View
                    android:layout_width="2dp"
                    android:layout_height="30dp"
                    android:alpha="0.4"
                    android:background="@color/ui_divider" />

                <View
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    android:layout_weight="1" />

                <View
                    android:layout_width="2dp"
                    android:layout_height="30dp"
                    android:alpha="0.4"
                    android:background="@color/ui_divider" />

                <View
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    android:layout_weight="1" />

                <View
                    android:layout_width="2dp"
                    android:layout_height="30dp"
                    android:alpha="0.4"
                    android:background="@color/ui_divider" />

                <View
                    android:layout_width="0dp"
                    android:layout_height="0dp"
                    android:layout_weight="1" />
            </LinearLayout>

            <!-- Слой 2: Индикатор-палка (Средний) -->
            <!-- Здесь можно менять ВЫСОТУ палки (layout_height) -->
            <View
                android:id="@+id/nav_slider"
                android:layout_width="0dp"
                android:layout_height="4dp"
                android:layout_gravity="bottom"
                android:layout_marginBottom="2dp"
                android:background="@drawable/bg_nav_indicator" />

            <!-- Слой 3: Иконки (Верхний слой) -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:orientation="horizontal">

                <!-- Кнопка "Календарь/Расписание" -->
                <ImageView
                    android:id="@+id/nav_today"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:clickable="true"
                    android:focusable="true"
                    android:padding="12.2dp"
                    android:scaleType="fitCenter"
                    android:src="@drawable/note_text_svgrepo_com"
                    app:tint="@color/ui_text_main" />

                <!-- Кнопка "Поиск" -->
                <ImageView
                    android:id="@+id/nav_search"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:clickable="true"
                    android:focusable="true"
                    android:padding="14dp"
                    android:scaleType="fitCenter"
                    android:src="@drawable/inbox_search_svgrepo_com"
                    app:tint="@color/ui_text_main" />

                <!-- Кнопка "Новости" -->
                <ImageView
                    android:id="@+id/nav_news"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:clickable="true"
                    android:focusable="true"
                    android:padding="13dp"
                    android:scaleType="fitCenter"
                    android:src="@drawable/mail_send_svgrepo_com"
                    app:tint="@color/ui_text_main" />

                <!-- Кнопка "Профиль" -->
                <ImageView
                    android:id="@+id/nav_profile"
                    android:layout_width="0dp"
                    android:layout_height="match_parent"
                    android:layout_weight="1"
                    android:clickable="true"
                    android:focusable="true"
                    android:padding="14dp"
                    android:scaleType="fitCenter"
                    android:src="@drawable/user_svgrepo_com__1_"
                    app:tint="@color/ui_text_main" />

            </LinearLayout>
        </FrameLayout>
    </androidx.cardview.widget.CardView>

</androidx.constraintlayout.widget.ConstraintLayout>